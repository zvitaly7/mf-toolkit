{
  "name": "mf-toolkit",
  "private": true,
  "description": "Microfrontend Toolkit — a modular collection of build-time and runtime tools for microfrontend architectures.",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces"
  }
}
