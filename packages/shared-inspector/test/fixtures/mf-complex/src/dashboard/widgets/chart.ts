// recharts is NOT in the share-candidates list — should appear in
// resolvedPackages but NOT in report.candidates
import { LineChart } from 'recharts';

// STAR REEXPORT: @emotion/react via export *
export * from '@emotion/react';
export * from '@emotion/styled';
