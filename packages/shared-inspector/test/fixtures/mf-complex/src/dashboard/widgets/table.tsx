// DIAMOND: @tanstack/react-query also re-exported here
// → via should still resolve to 'direct' because use-auth.ts imports it directly
export { useReactTable } from '@tanstack/react-table';
export { useQuery } from '@tanstack/react-query';
