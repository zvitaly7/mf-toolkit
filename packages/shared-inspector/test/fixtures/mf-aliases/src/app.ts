// Imports via path aliases — direct mode would treat these as external packages.
// local-graph + tsconfigPath should follow them into local files.
import { Button } from '@components/Button';
import { useData } from '@hooks/useData';
// Also a direct external import
import React from 'react';
